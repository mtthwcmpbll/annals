---
applications:
- name: annalist
  buildpacks:
  - https://github.com/cloudfoundry/java-buildpack.git
  routes:
  - route: annalist.apps.snowfort.com
  services:
  - annals-mongodb
  env:
    JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 10.0.+ } }'
    SPRING_PROFILES_ACTIVE: prod